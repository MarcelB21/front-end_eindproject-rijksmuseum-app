import './ObjectInfo.css';
import React from 'react';

const ObjectInfo = () => {

    try {

    } catch (e) {
        console.error(e);
    }

    return (
        <div>

        </div>
    );
};

export default ObjectInfo